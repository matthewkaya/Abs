# Secrets & env
.env
.env.local
.env.*.local
secrets/
*.pem
*.key
# 015 — manifest signing private key NEVER in repo
manifest-keys/

# 015 — exception: manifest_pubkey.pem GOMULU olmali (musterinin verify icin gerekli)
!core/backend/app/update/manifest_pubkey.pem

# Python
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
.eggs/
build/
dist/
.venv/
venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Data
*.sqlite
*.sqlite3
*.db
core/backend/data/
infra/data/

# OS / editor
.DS_Store
.idea/
.vscode/
*.swp

# Docker (local only)
infra/caddy-data/
infra/caddy-config/
site/

# Node / Next
node_modules/
.next/
out/
.turbo/
.swc/
*.tsbuildinfo
playwright-report/
test-results/
playwright/.cache/

# Q8 worker artifacts (kept locally only)
.playwright-mcp/
.audit/
.fs-scan-allowlist.snapshot

# Internal worker briefs — founder ↔ worker coordination, not for distribution
_agent-tasks/

# Generated research assets (social previews, image experiments, scratch)
_research/

# Per-customer SSH deploy keys + license tokens — NEVER commit
customer-keys/

# Abandoned eval methodologies — kept locally as audit trail only
artifacts/promise_verify/sprint_13_winrate.*
artifacts/promise_verify/opus_v_gptoss_*
artifacts/promise_verify/sonnet_v_gptoss_*
artifacts/promise_verify/winrate_consensus*
artifacts/promise_verify/q12_latency_rewrite_round_summary.md

# Local session state
.claude/scheduled_tasks.lock
.claude/state/
core/backend/core/
data/
logs/

# Helm dependency artifacts — regenerated by CI via `helm dependency update`
infra/helm/abs/charts/
infra/helm/abs/Chart.lock
infra/.cf-deploy-token
