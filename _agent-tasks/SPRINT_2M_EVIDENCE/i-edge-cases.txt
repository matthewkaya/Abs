# Sprint 2M FAZ I — Edge Cases + Negative Tests

## I1 License invalid
  Setup already completed → 409 (state machine block).
  Fake JWT specific reject test: setup reset gerekirdi, sim skip.

## I2 Auth fail-closed (UAT-009 regression)
  docker compose stop backend (5s wait)
  → /admin/dashboard: 200 OK (34KB HTML)  FAIL-CLOSED IHLALI
  → /healthz: 502 (Caddy upstream down, beklenen)

  Bug 2M-025 P0 — Landing service backend down olsa da /admin/* render eder.
  Next.js getServerSideProps / middleware admin auth check yok. Müşteri shell
  yükler, XHR çağrıları fail eder, "panel bozuk gibi" hisseder.

## I4 Auth login rate limit (UAT-041 regression)
  10 sequential POST /auth/login wrong password:
    attempts 1-10: ALL 401 (DEGIL 429 cap)
  FAIL — Brute force korumasız. UAT-041 5/min cap NOT ENFORCED.
  Bug 2M-024 P1 — Rate limit middleware /auth/login üzerinde yok ya da
  threshold çok yüksek (per-IP veya per-email değil).

## I3 Provider all invalid → graceful (FAZ G3 audit edildi)
  HTTP 200 SSE + TR fallback (Bug 2M-017, 2M-018) — bkz FAZ G evidence.

## I5 Formula injection / Excel upload
  /v1/excel/upload → 404 (endpoint bu repo'da yok, sibling project özelliği).
  SKIP — Sprint 2M scope dışı.

## I6 /v1/admin/users (RLS app-level)
  → 200 {"users":[],"total":0} (sim tenant=sim, user yok)
  RLS app-level guard çalışıyor (cross-tenant query yok, tenant_slug filter
  aktif). Postgres RLS yokken bile ORM-level isolation OK.

## I7 SQL injection (query param)
  ?limit=1 OR 1=1 → URL encoding sorunu (curl 000 connection error).
  Backend Literal[int] tipinde, 422 validation reject etmeli. Daha kapsamlı
  test SQLMap gibi tool ile yapılır — out of scope.

## I-NEW: Postgres RLS defense-in-depth (Sprint 2K vs customer compose)
  Sim DB URL: sqlite:////app/data/abs.db
  Customer compose default: DATABASE_URL unset → SQLite fallback
  Sprint 2K Postgres RLS migration aktif degil customer ortaminda!

  Bug 2M-026 P0 — Customer compose Postgres servisi YOK, SQLite default.
  Sprint 2K defense-in-depth katmani kayboluyor. KVKK risk:
  - PostgreSQL RLS GUC tenant_id filter yok
  - Sadece app-level (Cerbos + ORM) tenant guard
  - Bypass mümkün (ORM filter unutulursa veya raw SQL kullanilirsa)
  Cozum: infra/docker-compose.customer.yml'a Postgres 16 service ekle,
  Sprint 2K migration default, SQLite legacy fallback.

## Ozet
  License state machine 409 OK
  Admin /users RLS app-level calisiyor
  X Auth fail-closed UAT-009 BROKEN (P0)
  X Auth login rate limit UAT-041 BROKEN (P1)
  X Postgres RLS Sprint 2K defense-in-depth customer compose'da yok (P0)
