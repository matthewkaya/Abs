FAZ B — GHCR namespace alignment (Sprint 2N.2)
================================================

ROOT CAUSE (from v1.0.2 release run 25910970477):
  Build + push container images (landing) → FAIL
  ERROR: failed to push ghcr.io/enzoemir1/abs-landing:1.0.2: denied:
    permission_denied: The requested installation does not exist.

Workflow GITHUB_TOKEN is scoped to automatiabcn/abs (the repo owner).
It cannot push to a different account's namespace (enzoemir1/*)
without a founder-supplied PAT installed on that account.

CHANGES (6 files):

1. .github/workflows/release.yml
   - 8 occurrences of ghcr.io/enzoemir1/abs- → ghcr.io/automatiabcn/abs-
   - Header comments rewritten to reflect aligned namespace + removed
     cross-namespace cosign caveat (signing now hits same org).

2. infra/docker-compose.customer.yml
   - backend + landing + email-cron images now templated:
     ghcr.io/${ABS_GHCR_NAMESPACE:-automatiabcn}/abs-*:${ABS_VERSION:-latest}
   - Header docs ABS_GHCR_NAMESPACE env var with pre-2N.2 override note.

3. docker-compose.yml
   - Same parameterization for dev/founder local stack.

4. scripts/release.sh
   - GHCR_USER default: enzoemir1 → automatiabcn.
   - Header comment updated.

5. scripts/customer_onboard.sh
   - PAT resource-owner doc string updated.

6. core/backend/tests/test_q12_ip_hardening_r3_image_only.py
   - Customer compose image assertion loosened to accept template form
     and either namespace literal (regression-safe across pre/post 2N.2).
   - release.sh namespace assertion loosened similarly.

LOCAL VERIFY:
  tests/test_q12_ip_hardening_r3_image_only.py — 5/5 passed in 0.63s
  Full pytest regression deferred to FAZ E.

IMPACT FOR CUSTOMERS:
  - New installs on v1.0.3+ pull from ghcr.io/automatiabcn/abs-* by default.
  - Customers pinned to v1.0.1 / v1.0.0 (enzoemir1/*) keep working via
    ABS_GHCR_NAMESPACE=enzoemir1 in their .env until they upgrade.
  - Founder retains manual override: GHCR_USER=enzoemir1 ./scripts/release.sh
    1.0.3 still publishes to the legacy namespace for dual-publish drills.
