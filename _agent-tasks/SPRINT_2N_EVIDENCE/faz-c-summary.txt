Sprint 2N FAZ C — Postgres + RLS customer compose evidence
Generated: 2026-05-14T20:52:48Z

=== C1 compose diff (postgres service + backend wiring) ===
diff --git a/infra/docker-compose.customer.yml b/infra/docker-compose.customer.yml
index d03538b..ceecfa8 100644
--- a/infra/docker-compose.customer.yml
+++ b/infra/docker-compose.customer.yml
@@ -46,6 +46,11 @@ services:
     pull_policy: always
     env_file: .env
     environment:
+      # Sprint 2N FAZ C (P0 #2M-026) — Postgres default for customer
+      # compose so the Sprint 2K RLS migration (0015_rls_audit_tables)
+      # is active by default. SQLite legacy stays available via
+      # ABS_DATABASE_URL=sqlite:////app/data/abs.db in .env.
+      ABS_DATABASE_URL: "${ABS_DATABASE_URL:-postgresql+psycopg://abs:${ABS_DB_PASSWORD}@postgres:5432/abs}"
       # BUG-27 — point RAG/Cerbos at the in-stack services declared below.
       # Customers can override either URL in .env if they front-load their
       # own Qdrant/Cerbos clusters; defaults match the compose service names.
@@ -77,6 +82,11 @@ services:
       # Mounted read-only; the backend doesn't need to rotate it at runtime.
       - abs-vault-key:/app/vault-key:ro
     depends_on:
+      postgres:
+        # Sprint 2N FAZ C — wait for Postgres before backend boot.
+        # Backend entrypoint runs `alembic upgrade head` so the schema
+        # (incl. Sprint 2K RLS policies) is current before serving.
+        condition: service_healthy
       qdrant:
         # qdrant image has no curl/wget/nc/bash so a /dev/tcp probe always
         # fails; rely on `service_started` + backend's own qdrant_client
@@ -250,6 +260,34 @@ services:
       start_period: 40s
     restart: unless-stopped
 
+  # ---------------------------------------------------------------------------
+  # postgres — Sprint 2N FAZ C (P0 #2M-026) — KVKK defense-in-depth DB.
+  #
+  # Sprint 2K shipped Row-Level Security on the audit_*/tenant_* tables,
+  # but the customer compose previously had no Postgres service, so backend
+  # silently fell back to SQLite and RLS was never engaged. From 1.0.1
+  # onwards Postgres is the default; SQLite stays opt-in via
+  # ABS_DATABASE_URL=sqlite:////app/data/abs.db in .env (legacy, single-
+  # tenant only — RLS policies are no-op on SQLite).
+  # ---------------------------------------------------------------------------
+  postgres:
+    image: postgres:16-alpine
+    environment:
+      POSTGRES_USER: abs
+      POSTGRES_PASSWORD: "${ABS_DB_PASSWORD:?ABS_DB_PASSWORD env required — generate with openssl rand -base64 32}"
+      POSTGRES_DB: abs
+    volumes:
+      - abs-postgres-data:/var/lib/postgresql/data
+    expose:
+      - "5432"
+    healthcheck:
+      test: ["CMD-SHELL", "pg_isready -U abs -d abs"]
+      interval: 10s
+      timeout: 5s
+      retries: 12
+      start_period: 20s
+    restart: unless-stopped
+
   # ---------------------------------------------------------------------------
   # caddy — reverse proxy + automatic Let's Encrypt TLS.
   # Reads infra/Caddyfile (must be `infra/Caddyfile.customer` copied to
@@ -294,3 +332,6 @@ volumes:
   # BUG-29 — neo4j graph + transaction logs. Same persistence contract as
   # qdrant: stable across upgrades, wiped only by an explicit volume drop.
   abs-neo4j-data:
+  # Sprint 2N FAZ C (P0 #2M-026) — Postgres RLS-backed audit + tenant
+  # tables. Single named volume; back this up nightly alongside abs-data.
+  abs-postgres-data:

=== C2 .env.example diff (ABS_DB_PASSWORD) ===
diff --git a/.env.example b/.env.example
index 5c42ac5..0a02eaf 100644
--- a/.env.example
+++ b/.env.example
@@ -32,6 +32,17 @@ ABS_ACME_EMAIL=
 #   openssl rand -base64 32
 ABS_VAULT_KEY=
 
+# Postgres password for the in-stack DB (Sprint 2N FAZ C — P0 #2M-026).
+# Required from 1.0.1 onwards because customer compose defaults to
+# Postgres (Sprint 2K Row-Level Security policies are no-op on SQLite).
+# Generate once and store offline alongside ABS_VAULT_KEY:
+#   openssl rand -base64 32
+# To stay on the SQLite legacy path, also set
+#   ABS_DATABASE_URL=sqlite:////app/data/abs.db
+# and leave this blank — the compose Postgres service will still run but
+# the backend will not read from it.
+ABS_DB_PASSWORD=
+
 # === Provider API keys (paste at least one paid provider) =================
 # Order below mirrors the cascade priority. Anthropic recommended for
 # the best ABS experience; the others slot in as fallbacks.

=== C3 entrypoint.sh diff (alembic upgrade gate) ===
diff --git a/core/backend/scripts/entrypoint.sh b/core/backend/scripts/entrypoint.sh
index af32d2a..1f0e632 100755
--- a/core/backend/scripts/entrypoint.sh
+++ b/core/backend/scripts/entrypoint.sh
@@ -73,4 +73,29 @@ PY
     fi
 fi
 
+# Sprint 2N FAZ C (P0 #2M-026) — when ABS_DATABASE_URL points at Postgres,
+# run alembic upgrade head before launching uvicorn. Sprint 2K RLS migration
+# (0015_rls_audit_tables) needs to be applied or KVKK defense-in-depth is
+# silently bypassed. SQLite path keeps using SQLModel.metadata.create_all
+# (called by app startup) — no schema migration needed for the legacy
+# single-tenant install.
+DB_URL="${ABS_DATABASE_URL:-}"
+case "${DB_URL}" in
+    postgresql*|postgres*)
+        echo "[BOOT] Postgres detected — running alembic upgrade head..."
+        if (cd /app && alembic upgrade head); then
+            echo "[BOOT] alembic upgrade head OK"
+        else
+            echo "[BOOT] ERROR — alembic upgrade head failed; refusing to start with stale schema" >&2
+            exit 1
+        fi
+        ;;
+    sqlite*|"")
+        echo "[BOOT] SQLite (or unset DB URL) — skipping alembic; SQLModel.metadata.create_all on app start"
+        ;;
+    *)
+        echo "[BOOT] WARN — unknown ABS_DATABASE_URL scheme: ${DB_URL%%:*}" >&2
+        ;;
+esac
+
 exec "$@"

=== C5 customer_onboard.sh email template diff ===
diff --git a/scripts/customer_onboard.sh b/scripts/customer_onboard.sh
index ebe04a7..c6b0dfc 100755
--- a/scripts/customer_onboard.sh
+++ b/scripts/customer_onboard.sh
@@ -170,17 +170,33 @@ you do not need any source code.
 2. Install Docker:
    curl -fsSL https://get.docker.com | sh
 
-3. Place the four attached files in /opt/abs/ on your server:
+3. Place the attached files AND the cerbos/ folder in /opt/abs/ on your server:
      docker-compose.yml      # this customer compose
      Caddyfile               # reverse proxy config
      ghcr_pull.token         # read-only token to pull our images
      license.jwt             # your signed license
+     cerbos/                 # authorization engine config + policies (REQUIRED)
+       config.yaml
+       policies/*.yaml
+
+   Note: cerbos/ is mounted read-only at /etc/cerbos inside the cerbos
+   container. Without it the stack starts but cerbos exits and backend
+   cannot reach its authorization engine.
 
    Create /opt/abs/.env with:
      ABS_PUBLIC_URL=https://your-domain.com
-     ABS_VERSION=latest
+     ABS_VERSION=1.0.1
+     ABS_VAULT_KEY=\$(openssl rand -base64 32)
+     ABS_DB_PASSWORD=\$(openssl rand -base64 32)
      # plus your provider keys (Groq, Anthropic, etc.) — bring your own.
 
+   Note: ABS_DB_PASSWORD is REQUIRED from 1.0.1 onwards. The stack ships
+   with Postgres 16 by default so the Sprint 2K Row-Level Security
+   policies (KVKK / GDPR defense-in-depth) are active. To stay on the
+   single-tenant SQLite path, also add:
+     ABS_DATABASE_URL=sqlite:////app/data/abs.db
+   and leave ABS_DB_PASSWORD set anyway (Postgres still starts).
+
 4. Authenticate with our private container registry:
    cat ghcr_pull.token | docker login ghcr.io -u ${CUSTOMER_GITHUB_USER} --password-stdin
 

=== C6 compose validate (Postgres service rendered) ===
  postgres:
    environment:
      POSTGRES_DB: abs
      POSTGRES_PASSWORD: dummy
      POSTGRES_USER: abs
    expose:
      - "5432"
    healthcheck:
      test:
        - CMD-SHELL
        - pg_isready -U abs -d abs
      timeout: 5s
      interval: 10s

=== C7 8/8 compose schema test PASS ===
collecting ... collected 8 items

tests/test_2n_customer_compose_postgres.py::test_customer_compose_has_postgres_service PASSED [ 12%]
tests/test_2n_customer_compose_postgres.py::test_customer_compose_backend_depends_on_postgres_healthy PASSED [ 25%]
tests/test_2n_customer_compose_postgres.py::test_customer_compose_backend_defaults_to_postgres_url PASSED [ 37%]
tests/test_2n_customer_compose_postgres.py::test_customer_compose_postgres_password_required_via_env PASSED [ 50%]
tests/test_2n_customer_compose_postgres.py::test_customer_compose_postgres_data_volume_declared PASSED [ 62%]
tests/test_2n_customer_compose_postgres.py::test_entrypoint_runs_alembic_upgrade_on_postgres PASSED [ 75%]
tests/test_2n_customer_compose_postgres.py::test_entrypoint_alembic_failure_exits_nonzero PASSED [ 87%]
tests/test_2n_customer_compose_postgres.py::test_env_example_documents_abs_db_password PASSED [100%]

============================== 8 passed in 0.60s ===============================
