"""Groq v2 — extended responses API surface (tool_call deltas, JSON mode)."""

from __future__ import annotations

API_VERSION: str = "v2"
SUPPORTS_JSON_MODE: bool = True
SUPPORTS_TOOL_CALLS: bool = True
